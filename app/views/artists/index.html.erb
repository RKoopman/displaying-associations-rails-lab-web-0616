<ul>
  <% @artists.each do |artist| %>
    <li>
      <a href="artist_path(artist)"><%= artist.name %></a>
      <!-- <%= link_to artist.name, artist_path(artist) %> -->
      (<%= artist.song_count %> <%= "song".pluralize(artist.song_count) %>)
    </li>
  <% end %>
</ul>
